# 📊 REMINDER SYSTEM ARCHITECTURE & FLOW

## SYSTEM ARCHITECTURE

```
┌─────────────────────────────────────────────────────────────────┐
│                        NODE.JS SERVER                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────┐         ┌──────────────────┐             │
│  │   Express App    │         │  Cron Service    │             │
│  │                  │         │  (Every 5 mins)  │             │
│  │ Routes:          │         │                  │             │
│  │ - /api/auth      │         │ processPending   │             │
│  │ - /api/launch    │         │ Reminders()      │             │
│  │ - /api/checklist │         └────────┬─────────┘             │
│  │ - /api/reminder  │                  │                        │
│  └────────┬─────────┘                  │                        │
│           │                            │                        │
│           │                    ┌───────▼────────┐               │
│           │                    │ Reminder Service│               │
│           │                    │                │               │
│           │                    │ sendEmailReminder()            │
│           │                    │ createReminder()               │
│           └────────┬───────────┤ getPendingReminders()          │
│                    │           │ updateReminder()               │
│                    │           └────────┬────────┘               │
│            ┌───────▼──────────┐         │                       │
│            │   MongoDB        │◄────────┘                       │
│            │                  │                                 │
│            │ Collections:     │                                 │
│            │ - Users          │                                 │
│            │ - Launches       │                                 │
│            │ - Checklists     │                                 │
│            │ - Reminders      │                                 │
│            └──────────────────┘                                 │
│                                                                 │
│  ┌──────────────────┐    ┌──────────────────┐                  │
│  │   Nodemailer     │    │  Console Logs    │                  │
│  │  (Send Emails)   │    │  (For Testing)   │                  │
│  └──────────────────┘    └──────────────────┘                  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

         │
         │ Email
         │
         ▼
    ┌─────────┐
    │ User    │
    │ Inbox   │
    └─────────┘
```

---

## COMPLETE FLOW DIAGRAM

```
USER REGISTRATION
        │
        ▼
┌──────────────────┐
│ Create Launch    │ (Must be PAID user)
│ POST /api/launch │
└────────┬─────────┘
         │
         ├─→ Launch created ✅
         │
         ├─→ Checklist auto-generated 🔄
         │   ├─ Prepare PH page (-30 days)
         │   ├─ Hunter outreach (-21 days)
         │   ├─ Teaser content (-14 days)
         │   ├─ Email draft (-7 days)
         │   ├─ Launch live (0 days)
         │   └─ Thank you post (+1 day)
         │
         └─→ Reminders auto-created 📬 (6 total)
             ├─ reminder_1: sendAt = -30 days
             ├─ reminder_2: sendAt = -21 days
             ├─ reminder_3: sendAt = -14 days
             ├─ reminder_4: sendAt = -7 days
             ├─ reminder_5: sendAt = 0 days
             └─ reminder_6: sendAt = +1 day

                    ⏳ WAIT FOR DUE DATES ⏳

CRON JOB RUNS (Every 5 minutes)
        │
        ▼
┌──────────────────────────────────────┐
│ Check for pending reminders          │
│ WHERE sent=false AND sendAt<=now     │
└────────┬─────────────────────────────┘
         │
    ┌────┴─────┐
    │ Found any?
    │
    YES (sendAt passed)
    │
    ▼
┌──────────────────────────────────────┐
│ For each pending reminder:           │
│                                      │
│ 1. Get user email                    │
│ 2. Create email HTML                 │
│ 3. Send via Nodemailer               │
│ 4. Mark sent=true                    │
│ 5. Set sentAt=now                    │
└──────────────────────────────────────┘
    │
    ▼
┌──────────────────────┐
│ Email Sent ✅        │
│ to user@email.com    │
└──────────────────────┘

    Or (without email config)
    │
    ▼
┌──────────────────────────┐
│ Console Log:             │
│ ✅ Email sent to ...     │
│ (Still marked as sent)   │
└──────────────────────────┘

    Next cron cycle:
    └─ No duplicate sends (sent=true prevents re-send)
```

---

## DATA RELATIONSHIPS

```
┌──────────┐
│  User    │
│ john@... │
└────┬─────┘
     │
     │ userId
     │
     ▼
┌──────────────┐
│  Launch      │
│ My App       │
│ 2026-02-15   │
└────┬─────────┘
     │
     ├─ launchId ──→ ┌────────────┐
     │               │ Checklist  │
     │               │ Item 1     │
     │               │ due: 1/16  │
     │               └────┬───────┘
     │                    │ checklistId
     │                    │
     │                    ▼
     │              ┌────────────┐
     │              │ Reminder 1 │
     │              │ sendAt:1/16│
     │              │ sent:false │
     │              └────────────┘
     │
     └─ launchId ──→ ┌────────────┐
                     │ Checklist  │
                     │ Item 2     │
                     │ due: 1/26  │
                     └────┬───────┘
                          │
                          ▼
                     ┌────────────┐
                     │ Reminder 2 │
                     │ sendAt:1/26│
                     │ sent:false │
                     └────────────┘
```

---

## CRON JOB TIMING

```
Time        Action
────────────────────────────────────────────
00:00:00    🟢 Job runs
00:05:00    🟢 Job runs
00:10:00    🟢 Job runs
...         ...
12:00:00    🟢 Job runs
12:05:00    🟢 Job runs
...         ...
23:55:00    🟢 Job runs
00:00:00    🟢 Job runs (next day)

Frequency: Every 5 minutes, 24/7
Max delay: ~5 minutes
Pattern: */5 * * * * (Unix cron)
```

---

## EMAIL TEMPLATE FLOW

```
CREATE REMINDER
        │
        ▼
┌────────────────────────────────┐
│ sendAt time arrives            │
└────────┬───────────────────────┘
         │
         ▼
┌────────────────────────────────┐
│ Cron job finds reminder        │
│ (sent=false, sendAt<=now)      │
└────────┬───────────────────────┘
         │
         ▼
┌────────────────────────────────┐
│ Send email with template       │
│                                │
│ ┌──────────────────────────┐   │
│ │ Subject:                 │   │
│ │ 🚀 GoToLaunch Reminder   │   │
│ └──────────────────────────┘   │
│ ┌──────────────────────────┐   │
│ │ Hi [User Name],          │   │
│ │                          │   │
│ │ This is your reminder:   │   │
│ │                          │   │
│ │ [Task Title]             │   │
│ │ [Description]            │   │
│ │                          │   │
│ │ [Button: View Launch]    │   │
│ └──────────────────────────┘   │
└────────┬───────────────────────┘
         │
         ▼
┌────────────────────────────────┐
│ Update Reminder in DB          │
│ sent = true                    │
│ sentAt = [current timestamp]   │
└────────────────────────────────┘
```

---

## API ENDPOINT FLOW

```
REQUEST: GET /api/reminder/user
         │
         ├─ Check auth token ✅
         │
         ├─ Get userId from token
         │
         └─ Query Reminder.find({ userId })
            │
            ▼
         RESPONSE:
         {
           reminders: [
             { id, message, sendAt, sent, sentAt },
             { id, message, sendAt, sent, sentAt },
             ...
           ]
         }


REQUEST: POST /api/reminder/send-pending
         │
         ├─ Query pending reminders
         │  WHERE sent=false AND sendAt<=now
         │
         ├─ For each reminder:
         │  ├─ Send email
         │  ├─ Set sent=true
         │  └─ Set sentAt=now
         │
         └─ Return stats: { sent: N, failed: 0 }


REQUEST: DELETE /api/reminder/:id
         │
         ├─ Check ownership
         │
         ├─ Delete from DB
         │
         └─ Return success message
```

---

## STATUS TRANSITIONS

```
REMINDER LIFECYCLE

[Created]
   │
   │ created with:
   │ - sent: false
   │ - sentAt: null
   │ - sendAt: [due date]
   │
   ▼
[Pending]
   │
   │ waiting for sendAt <= now
   │
   ▼
[Sending] ──→ Error ──→ [Pending] (retries next cycle)
   │
   │ email sent successfully
   │
   ▼
[Sent]
   │
   │ sent: true
   │ sentAt: [timestamp]
   │
   ├─→ User can delete
   │
   └─→ Won't send again
```

---

## INTEGRATION WITH OTHER SYSTEMS

```
┌─────────────────────────────────────────────────┐
│              Launch Created                     │
│  POST /api/launch/create                        │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
        ┌────────────────────┐
        │ Launch Service     │
        │ createLaunch()     │
        └────────┬───────────┘
                 │
         ┌───────┴──────────┐
         │                  │
         ▼                  ▼
    ┌─────────┐      ┌──────────────┐
    │ Launch  │      │ Checklist    │
    │ created │      │ Service      │
    │         │      │ auto-generate│
    └─────────┘      └──────┬───────┘
                            │
                     ┌──────┴──────┐
                     │             │
                     ▼             ▼
                 ┌──────────┐  ┌──────────────┐
                 │ 6 Items  │  │ Reminder     │
                 │ created  │  │ Service      │
                 │          │  │ creates 6    │
                 └──────────┘  │ reminders    │
                               └──────────────┘
```

---

## ERROR HANDLING

```
PROCESS ERROR

Cron Job Runs
         │
         ▼
Find Pending Reminders
         │
    ┌────┴──────┐
    │            │
   YES          NO
    │            │
    ▼            ▼
Send Email   [Done - No pending]
    │
    ├─ Success ──→ Mark sent=true ✅
    │
    └─ Failure ──→ Log error
                   Keep sent=false
                   Retry next cycle
                   (No data loss!)
```

---

## FILE STRUCTURE

```
src/
├── models/
│   └── Reminder.js           # Schema
│
├── services/
│   ├── reminder.service.js   # Business logic
│   │   ├─ createReminder()
│   │   ├─ getPendingReminders()
│   │   ├─ sendEmailReminder()
│   │   └─ processPendingReminders()
│   │
│   └── cron.service.js       # Scheduling
│       ├─ initReminderCron()
│       ├─ stopReminderCron()
│       └─ getCronStatus()
│
├── controllers/
│   └── reminderController.js # HTTP handlers
│
└── routes/
    └── reminderRoutes.js     # Endpoints
```

---

**This architecture ensures reliable, automated reminders with proper error handling and no duplicates!** ✨
